import TvBoard from "./tv-board";

export const metadata = {
  title: "Live board | Action Line-Up",
  description: "Live tournament board for the bar screen.",
};

export default function TvPage() {
  return <TvBoard />;
}
